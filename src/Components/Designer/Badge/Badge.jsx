import React, { useState } from 'react';

function Badge({ label, disabled, checked, onChange }) {

    return (
        <div className={"badge"}>
            
        </div>
    );
}

export default Badge;
